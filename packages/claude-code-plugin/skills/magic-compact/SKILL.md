---
name: magic-compact
description: Compact this Claude Code session.
argument-hint: "[N]"
disable-model-invocation: true
---

Shim for running /magic-compact command. If you see this message, the shim has somehow failed and the command did not execute correctly. If so, alert the user and propose ways to debug installation of the `magic-compact` plugin.
